---
title: Cuentas y CVU
description: Alta de cuentas y CVU con /v1 y /wallet/v1, alias y saldos.
---

# Cuentas y CVU

## Entornos

| | URL | Key |
|--|-----|-----|
| Homolog | `https://api.dev.gallo-pay.com` | `gpk_test_…` |
| Prod | `https://api.prod.gallo-pay.com` | `gpk_live_…` |

## Dos familias

| Familia | Uso |
|---------|-----|
| `/v1/accounts` | API nativa: CVU, alias, balance, comprobantes |
| `/wallet/v1` | Fachada Bind: `Cuenta`, `CVU`, saldos PascalCase |

Mismo tenant vía `x-api-key`. No mezcles IDs entre capas sin mapear.

## Alta nativa

```bash
curl -X POST https://api.dev.gallo-pay.com/v1/accounts \
  -H "x-api-key: gpk_test_…" \
  -H "Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d '{
    "cuit": "20370994049",
    "titular": "JUAN PEREZ",
    "tipoPersona": "F",
    "alias": "gallo.juan.perez",
    "pep": false,
    "nationality": "AR"
  }'
```

Respuesta típica (201):

```json
{
  "cvu": "0000003100000000000147",
  "cuit": "20370994049",
  "titular": "JUAN PEREZ",
  "tipoPersona": "F",
  "alias": "gallo.juan.perez",
  "status": "active"
}
```

## Wallet — Cuenta → CVU

```bash
curl -X POST https://api.dev.gallo-pay.com/wallet/v1/Cuenta \
  -H "x-api-key: gpk_test_…" \
  -H "Content-Type: application/json" \
  -d '{
    "cuitCuil": "20370994049",
    "nombre": "Juan",
    "apellido": "Perez",
    "razonSocial": "Juan Perez",
    "email": "juan@example.com",
    "celular": "1168599999",
    "actividadAfip": "000012",
    "habilitado": true,
    "esPep": false,
    "esFatca": false,
    "esUif": false,
    "nacionalidad": "AR"
  }'

curl -X POST https://api.dev.gallo-pay.com/wallet/v1/CVU \
  -H "x-api-key: gpk_test_…" \
  -H "Content-Type: application/json" \
  -d '{ "idCuenta": 123, "nombreCVU": "Juan Perez", "aliasAutomatico": true }'
```

## Saldos

```bash
curl https://api.dev.gallo-pay.com/v1/accounts/0000003100000000000147/balance \
  -H "x-api-key: gpk_test_…"
```

Wallet: `SaldoActualByCVU`, `SaldoHistoricoByCVU`, `SaldosActuales`.

## Modo sweep (CVU primera recaudadora)

Opt-in por PSP (admin). El cash-in a CVUs de usuario se barre a una **CVU tesorería**.
La otra app ve el **saldo de cliente** (lo que pasó por esa CVU), no el ledger de la CVU (≈0).

En `GET /v1/accounts` y `GET /v1/accounts/:cvu`, la CVU de tesorería viene con `isTreasury: true`.
El balance de esa CVU también expone `isTreasury` (y `freeBalance` cuando el sweep está activo).

| Endpoint | Uso |
|----------|-----|
| `GET /v1/treasury` | Snapshot: pasivo clientes, `freeBalance` (informativo) |
| `GET /v1/treasury/positions/:cvu` | Saldo de cliente |
| `GET /v1/treasury/positions/:cvu/movements` | Movimientos del libro cliente |
| `GET /wallet/v1/Tesoreria` / `SaldoClienteByCVU/:cvu` | Fachada wallet |

Cash-out: `POST /v1/transfers` con `cvuDebito` = CVU del usuario. Tesorería puede
entrar/salir por su propia CVU sin afectar saldos mostrados a clientes.

Comprobantes manuales sobre CVU usuario también barren a tesorería (misma regla que
un cash-in). Si hay saldos atrapados en ledger de usuario: reconciliar desde el
panel del PSP o `POST /v1/psps/:id/treasury/reconcile-sweep`.

## Eventos

`account.created`, `account.alias.updated`, `account.credited`, `account.closed` — ver [Webhooks](/guias/webhooks).

En modo sweep, `account.credited` incluye `swept` y `treasuryCvu`; un solo evento por cash-in (no hay crédito extra por el P2P de barrido).
